name: Retrigger GOAT26 Database Checks

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/retrigger-goat26-database.yml'
  workflow_dispatch:

permissions:
  actions: write
  contents: read

jobs:
  dispatch:
    runs-on: ubuntu-latest
    steps:
      - name: Run GOAT26 workflows from latest main
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          for workflow in apply-goat26-database.yml audit-goat26-candidates.yml; do
            curl --fail-with-body --silent --show-error \
              --request POST \
              --url "https://api.github.com/repos/${GITHUB_REPOSITORY}/actions/workflows/${workflow}/dispatches" \
              --header "Accept: application/vnd.github+json" \
              --header "Authorization: Bearer ${GH_TOKEN}" \
              --header "X-GitHub-Api-Version: 2022-11-28" \
              --data '{"ref":"main"}'
          done