name: Phase 3 Whats New Owner Diagnostic

on:
  pull_request:
    branches: [main]
    paths:
      - 'assets/js/app-update-watcher.js'
      - 'assets/js/native-app-shell-stability.js'
      - 'scripts/test-native-shell-stability-whats-new-contract.mjs'
      - 'scripts/test-native-shell-stability-whats-new-owner.mjs'
      - '.github/workflows/phase-3-whats-new-owner-diagnostic.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: phase-3-whats-new-owner-${{ github.ref }}
  cancel-in-progress: true

jobs:
  whats-new-owner:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Validate runtime and proof syntax
        run: |
          node --check assets/js/app-update-watcher.js
          node --check assets/js/native-app-shell-stability.js
          node --check scripts/test-native-shell-stability-whats-new-contract.mjs
          node --check scripts/test-native-shell-stability-whats-new-owner.mjs
      - name: Run static ownership contract
        run: node scripts/test-native-shell-stability-whats-new-contract.mjs
      - name: Install browser runtime
        run: npm install --no-save --package-lock=false playwright@1.54.1 && npx playwright install --with-deps chromium
      - name: Start local app
        run: |
          python3 -m http.server 4173 --bind 127.0.0.1 > /tmp/whats-new-owner-server.log 2>&1 &
          for attempt in $(seq 1 20); do
            if curl --fail --silent http://127.0.0.1:4173/index.html > /dev/null; then exit 0; fi
            sleep 1
          done
          cat /tmp/whats-new-owner-server.log
          exit 1
      - name: Run mobile ownership proof
        run: |
          set -o pipefail
          node scripts/test-native-shell-stability-whats-new-owner.mjs 2>&1 | tee /tmp/native-shell-stability-whats-new-owner.log
      - name: Upload diagnostic
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: native-shell-stability-whats-new-owner-diagnostic
          path: |
            /tmp/native-shell-stability-whats-new-owner-report.json
            /tmp/native-shell-stability-whats-new-owner.log
            /tmp/whats-new-owner-server.log
          if-no-files-found: ignore
