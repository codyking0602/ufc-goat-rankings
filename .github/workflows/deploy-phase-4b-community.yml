name: Phase 4B Community Profiles — Paused

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - name: Phase 4B is paused
        run: |
          echo "Phase 4B community profiles are intentionally disabled while the Phase 4A app shell is stabilized."
          echo "Do not re-enable automatic deployment until startup is physically verified on iPhone."
