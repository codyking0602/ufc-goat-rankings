name: Phase 4B Community Profiles — Paused

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - name: Phase 4B remains preview-only
        run: |
          echo "Phase 4B is intentionally isolated on phase-4b-clean-preview."
          echo "Do not deploy to main until physical iPhone approval."