// Optional event-only fighter thumbnails.
// Existing ranked fighters automatically reuse DISPLAY_OVERRIDES thumbUrl/photoUrl.
// Add a WebP path here only when a real file exists in assets/picks/fighters/.
window.UFC_PICKS_PHOTOS = {
};
