(function () {
  // Central Fight Ledger file.
  // Existing direct fight/rivalry entries are currently loaded through assets/data/compare-profiles.js
  // because the legacy compare packs contain both profiles and ledger entries.
  // New direct fight/rivalry entries should be moved here as the compare data is fully consolidated.
  window.FIGHT_LEDGER_FILE_READY = true;
})();
