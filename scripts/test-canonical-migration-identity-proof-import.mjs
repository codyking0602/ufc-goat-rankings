// The canonical migration ownership proof is imported by test-profile-login-owner-delegation.mjs,
// which is already a required Startup Architecture Gate browser step.
export const canonicalMigrationIdentityProofWired=true;
